export interface NoDataProps {
  hasData: boolean;
}

// export const NoData: React.FC<React.PropsWithChildren<NoDataProps>> = ({
//   hasData,
//   children,
// }) => {
//   return hasData ? <>{children}</> : <div>No data</div>;
// };
